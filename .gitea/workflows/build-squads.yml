name: Build Squads Service

on:
  push:
    branches:
      - 'develop'
    paths:
      - 'packages/squad/**'
  pull_request:
    branches:
      - 'develop'
    paths:
      - 'packages/squad/**' 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

  publish:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker BuildX
        uses: docker/setup-buildx-action@v3
        with:
          config-inline: |
            [registry."10.43.102.250:3000"]
              http = true
              insecure = true

      - name: Login into registry
        uses: docker/login-action@v3
        with:
          registry: 10.43.102.250:3000
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./packages/squad/Containerfile
          push: true
          tags: 10.43.102.250:3000/heimdall/squads-service:0.1.0,10.43.102.250:3000/heimdall/squads-service:latest
